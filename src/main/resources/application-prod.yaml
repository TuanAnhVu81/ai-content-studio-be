spring:
  config:
    activate:
      on-profile: prod

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

  data:
    redis:
      ssl:
        enabled: ${REDIS_SSL_ENABLED:false} 

logging:
  level:
    com.portfolio.aicontentstudio: INFO
    org.springframework.web: WARN
