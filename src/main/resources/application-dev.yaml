spring:
  config:
    activate:
      on-profile: dev

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        
logging:
  level:
    com.portfolio.aicontentstudio: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
